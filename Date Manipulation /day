-- SQL Query: Extracting Job Posting Information

SELECT job_id,
       EXTRACT(DAY FROM job_posted_date) AS posted_day
FROM job_postings_fact;

-- Explanation:
-- job_id: Unique identifier for each job posting.
-- EXTRACT(DAY FROM job_posted_date): Extracts the day of the month from the job_posted_date column.
-- job_postings_fact: The table containing all job posting data.
